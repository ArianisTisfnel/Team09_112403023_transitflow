# Environment variables — never commit API keys
.env

# Instructor reference copies — not distributed to students
*_og.py
*_og.sql
*_og.cypher

# Claude Code session files
.claude/

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.egg
*.egg-info/

# Virtual environments
venv/
.venv/
env/
ENV/

# Distribution / packaging
dist/
build/

# Testing and type checking
.pytest_cache/
.mypy_cache/
.coverage
htmlcov/

# Logs
*.log

# Database exports and dumps — never commit data exports
*.dump
*.sql.dump
*.sql.bak
*.csv

# Gradio — created automatically when the chat UI runs
flagged/
.gradio/
gradio_cached_examples/

# Jupyter
.ipynb_checkpoints/

# IDE
.vscode/
.idea/
*.iml

# OS
.DS_Store
Thumbs.db
desktop.ini

# Work Allocation Report — contains PII (names, student IDs, emails).
# Per instructor announcement: submit via EEClass ONLY, never on public GitHub.
Team09_WORK_ALLOCATION.md
TEAM9_WORK_ALLOCATION.md

# Local scratch / temporary helpers — not part of the submission
_run_ui_alt.py
docs/額外補充.md

# Peer Review Reports — individual, confidential, EEClass-only (never in repo)
peer_review/
*PEER_REVIEW*

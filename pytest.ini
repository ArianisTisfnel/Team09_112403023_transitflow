[pytest]
# importlib import mode lets test files in tests/unit and tests/integration
# share the same basename (e.g. test_execute_booking.py in both) without clashing.
addopts = --import-mode=importlib
testpaths = tests
